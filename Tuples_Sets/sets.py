# unordered data with no order or duplicates

set = {1,2,3}

# add, remove, copy, clear, 

set.add(23)
set.remove(1)
set.discard(2)
set.copy

print(set)

# set math

# | used for union so allows you to have two sets and print out a list of unique values - no duplicates between the sets
# & is used to grab two sets and only print out values in both sets